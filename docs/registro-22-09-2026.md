# Registro — 22/09/2026 — revisão pré-publicação e site no ar

Sessão de trabalho com o Claude Code. Objetivo do dia: publicar o site em
`prescricao.conversion.com.br` (registro DNS `A` `prescricao` → `148.230.73.9`, DNS only).
Horários em Brasília.

## 1. Revisão do repositório

Estado inicial: `main` = `origin/main` em `27ca6d6`, árvore limpa. A home estava íntegra:
todo asset referenciado existe, todo ícone do sprite resolve, toda classe do HTML tem
regra CSS, `html-validate` sem erros reais (só o estilo do doctype minúsculo), console
limpo no navegador. Sobras do Whitebook encontradas e corrigidas no commit `e4e7ddb`:

| Onde | Problema | Correção |
|---|---|---|
| `404.html` | Página de erro ainda com título, lockup, menu, login e destinos do Whitebook | Header da home, copy de Prescrição e três botões para destinos que existem (`/`, `/#como-funciona`, `/#duvidas`) |
| `tools/afya-prescricao-lockup.js` | Caminhos fixos de outra máquina (`C:/Users/mathe/...`) | Resolve a partir da raiz do repo; exige argumentos; instrução de instalação da dependência. Verificado: recompõe o lockup com entreletra −4,51 px e mesmas dimensões |
| `nginx.conf` | Três redirects do Whitebook (`/para-voce/`, `/previa`, `/preview`), um deles levando a 404 | Removidos |
| `index.html` | Preload do hero só com o 1280w enquanto o navegador escolhia o 1920w (download desperdiçado); `<body class="">` | `imagesrcset`/`imagesizes` no preload; atributo vazio removido |
| `assets/images/hero/hero-plantao-v4-*` | Sem uso (já apontado no README) | Apagados |
| `Dockerfile` | Comentário "Afya Whitebook" | Ajustado |
| `.gitignore` | — | `.playwright-mcp/` (capturas de verificação local) |

Ficaram de propósito, listados no README ("Origem"): ~40 assets do Whitebook sem uso
(benefícios, equipe, depoimentos, telas, vídeo, personas, selos de download, ícones
soltos) e ~350 linhas de `script.js` de componentes que a home não tem. Decisão adiada
para quando as páginas internas forem definidas.

## 2. Diagnóstico do ambiente

Com o DNS já resolvendo, o domínio respondia `503 no available server` em HTTPS com o
`TRAEFIK DEFAULT CERT` e `404` em HTTP. Primeira leitura (errada): app criada no Coolify
sem container saudável. Leitura correta, depois de entrar no VPS por SSH e consultar o
banco do Coolify: **não existia aplicação alguma** para o domínio; o proxy deste VPS
responde 503 para qualquer hostname desconhecido em HTTPS (router pega-tudo de outro
app). Dockerfile e healthcheck são idênticos aos do Whitebook, que roda no mesmo VPS.

## 3. Provisionamento (15:49 – 15:59)

Tudo pela API do Coolify de dentro do VPS, com token temporário `claude-cli-prescricao`
criado via `artisan tinker` e revogado ao fim. Detalhes e comandos em `DEPLOY_VPS.md` §6.

1. Projeto `prescricao` (uuid `cldesnd0gynu5hx7jx7dikqq`).
2. Deploy key ed25519 `coolify-vps-srv915770-prescricao`: pública no GitHub (read-only),
   privada no Coolify.
3. Aplicação `prescricao-site` (uuid `4cbwuxavvyzmsfe4vrw0qoak`): Dockerfile, base `/`,
   porta 80, domínio `https://prescricao.conversion.com.br`.
4. Primeiro deploy (`POST /applications/{uuid}/start`; o `GET /deploy` do runbook do
   Whitebook responde 405 no Coolify 4.3.23): 15:56:18 → 15:57:03, container `healthy`.
5. Webhook de push no GitHub (id `683858609`) assinado com o secret da aplicação.
6. Push `0f46941` (README) às 15:58:56: entrega `200` no GitHub, deploy 15:58:56 →
   15:59:33, container trocado. Autodeploy validado.

## 4. Verificação externa (após o deploy)

| Checagem | Resultado |
|---|---|
| Certificado | `CN=prescricao.conversion.com.br`, emissor Let's Encrypt |
| `GET /` | 200, `X-Robots-Tag: noindex, nofollow` |
| `GET /x` | 404 com a página de erro de Prescrição |
| `GET /robots.txt` | `Disallow: /` |
| `http://` | 302 → `https://` |
| `index.html` e `404.html` em produção | byte a byte iguais ao `HEAD` |
| TLS pelo trust store do sistema | válido |

## 5. Limpeza

Token de API revogado e apagado do VPS; cópia em disco da deploy key e arquivos de
trabalho removidos; nenhum segredo no repositório. O secret do webhook passou pelo log
da sessão — rotacionar se desejado.

## 6. Decisões e próximos passos

- Ambiente segue **noindex** nos três pontos (robots, meta, header) até o lançamento.
- Domínio de lançamento: `prescricaodigital.com` (reunião de 15/09), grafia a confirmar.
- Pendências atualizadas no `README.md` ("Pendências").
